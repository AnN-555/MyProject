this is a sharing git repo homework
mkdir <name> : create folder <name>
cd <name>: go to folder <name> from current position
git init -b main: create a git repo at <name> is main branch
git add .
git commit -m "comment line"
git diff to check for differences
git log: check HEAD, changes, stats